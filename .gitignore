logs/
.claudewarp-templates/
.claudewarp-skills/
CLAUDE_WARP_UPDATE_LOG.md
# Local self-host symlinks created by scripts/dev.sh selfhost (keep repo a pure source)
.claude/skills/
# Process scratch: research docs, per-PR contract/GOAL records, verifiers (keep repo clean)
working/
# Cross-session closure ledger: local runtime data (per-checkout persistence), like a log
.claudewarp/ledger.jsonl
# MkDocs build output (the published site is built in CI, never committed)
site/
