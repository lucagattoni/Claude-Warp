# Constitution — {{PROJECT_NAME}}

Standing, non-negotiable principles that govern every contract in this project. The
`/claude-warp-contract` critical pass (Phase 6) and readiness gate (Phase 7) validate each
contract against the MUST principles below. A MUST violation is **non-dilutable**: a contract
may not reinterpret a principle to pass — changing a principle is a separate, explicit edit
to this file.

> This file ships **unfilled**. Until you replace the example rows below, the constitution
> check is a no-op (contracts negotiate exactly as before). Fill it in to switch governance on.

## Principles

<!-- Each principle is one normative statement. MUST = blocking; SHOULD = advisory.
     Replace or delete every UNFILLED row before this constitution takes effect. -->

- [ ] **P1 (MUST)** — # UNFILLED — e.g. "All public API changes ship with tests."
- [ ] **P2 (MUST)** — # UNFILLED — e.g. "No secrets, tokens, or PII in source or logs."
- [ ] **P3 (SHOULD)** — # UNFILLED — e.g. "User-facing copy stays in the established voice."
- [ ] **P4 (MUST)** — # UNFILLED — e.g. "No change is 'done' without an independent verifier passing."

## Amendments

<!-- Log principle changes here; an amendment is an explicit governance act, not a side effect. -->
- [{{INSTALLED_AT}}] Constitution scaffolded (unfilled).
